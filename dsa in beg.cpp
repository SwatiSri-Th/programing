#include<stdio.h>
#include<stdlib.h>
struct node
{
	int data;
	struct node *link;
};
struct node *addatbeg(struct node *start);
int main()
{
	struct node *p;
	p=(struct node *)malloc(sizeof(struct node));
	p=data=6;
	p->link=start;
	start=p;
}
